interface:
  display_name: "Grilling"
  short_description: "Stress-test a plan one decision at a time"
  default_prompt: "Use $grilling to stress-test this plan one question at a time, with a recommended answer for each decision."

policy:
  allow_implicit_invocation: true

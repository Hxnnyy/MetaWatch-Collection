# Changelog

## 2026-04-15

- Installed from https://github.com/upstash/context7 (`skills/context7-cli`). Replaces the Context7 MCP server across Claude Code, Codex, Gemini CLI, Copilot CLI, and VS Code on both Windows and WSL — CLI approach is preferred for lower context bulk.
- Added self-improvement footer per `~/.agents/skills/_meta/skill-management.md`.

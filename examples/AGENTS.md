# Project agent instructions

These instructions apply only within this project. Preserve user- and tool-level rules; where instructions conflict, follow the more specific applicable rule and surface material ambiguity.

## Working agreement

- Inspect existing code, documentation, tests, and repository state before changing files.
- Keep changes within the requested scope and preserve unrelated work.
- Make uncertainty and public interfaces explicit.
- Prefer the fewest concepts that fully solve the current problem.
- Add durable automated coverage for meaningful behaviour changes, or record explicit test debt.
- Run relevant checks and summarize changed files, verification, and remaining risks.
- Ask before destructive, irreversible, externally visible, or permission-expanding actions.

## Project commands

Document real, repository-supported commands here:

- Setup: `<command>`
- Test: `<command>`
- Lint: `<command>`
- Build: `<command>`

Merge this example into project-specific instructions. Do not overwrite an existing `AGENTS.md`.
